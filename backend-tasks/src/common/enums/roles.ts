// Enumeración que define los roles disponibles en el sistema
export enum Roles {
    // Rol de administrador con permisos completos
    admin = "admin",
    // Rol de usuario regular con permisos limitados
    user = "user",
}
