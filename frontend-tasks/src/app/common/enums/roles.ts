// Enumeración que define los roles de usuario disponibles en la aplicación
export enum Roles {
  admin = 'admin', // Rol de administrador con acceso completo a todas las funcionalidades
  user = 'user', // Rol de usuario básico con permisos limitados para gestionar sus propias tareas
}
